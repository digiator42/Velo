[package]
name = "new-features"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
velo = { path = "../../crates/velo" }
wasm-bindgen = { workspace = true }
console_error_panic_hook = "0.1"
