[package]
name = "velo"
version = "0.1.0"
edition = "2021"
description = "The unified, high-performance fine-grained reactive full-stack web framework."

[dependencies]
# Pull in your internal engine parts locally
velo_core = { path = "../core" }
velo_dom = { path = "../dom" }
velo_router = { path = "../router" }
velo_macro = { path = "../macro" }