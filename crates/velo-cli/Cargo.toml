[package]
name = "velo-cli"
version = "0.1.0"
edition = "2021"
description = "Velo CLI — scaffold, dev, build"
license = "MIT"
repository = "https://github.com/digiator42/Velo"

[[bin]]
name = "velo"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
